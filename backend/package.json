{
  "name": "taskflow-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "ts-node-dev --respawn lambdas/local-server.ts",
    "lint": "eslint 'lambdas/**/*.ts'",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/util-dynamodb": "^3.600.0",
    "jose": "^5.6.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^20",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
